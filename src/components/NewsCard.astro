---
import dayjs from 'dayjs';
const { content } = Astro.props;
const date = dayjs(content.publishedDate).format('YYYY.MM.DD');
console.log(content)
---

<a href={'/news/' + content.id} class="sokenkai__topiccard">
  <p>{date}</p>
  <h4>{content.tag}</h4>
  <h3>{content.title}</h3>
</a>
